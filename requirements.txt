# =========================
# Core Web Framework
# =========================
fastapi==0.110.0
# Combined into one line to avoid the version conflict
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# =========================
# AI / Deep Learning (CPU-only)
# =========================
# Using --index-url in Render's build command is better, 
# but for the file, we keep these compatible:
torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
numpy==1.26.4

# =========================
# Image Processing & DB
# =========================
pillow==10.1.0
motor==3.3.2
pymongo==4.6.1

# =========================
# Utilities & Logging
# =========================
python-dotenv==1.0.1
loguru==0.7.2
